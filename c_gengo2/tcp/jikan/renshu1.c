 #include <stdio.h>
 #include <time.h>

 int main(void)
{
  int pp,fp,n=0,first[1],end[1];
  pp=popen("ping 192.168.0.109 > text"n".txt","r");

  /* jikan shutoku */
  while(n<15){
    exec(fp,NULL); 
    n++;
  }

  /* keisan */
  while(n<15){
    fp=open("text"n".txt",O_RONLY);
    /* get the elements of hh:mm:ss from files */
    if(icmp_req nara){
      first=icmp_req; /*  first second stored */
      }

    if(n=14){
      end=icmp_req;
      }
    close(fp);
  }

  printf("sokuteishuryou\n");
  return 0;
}
